/**
 * App route group layout. Phase 2a adds the auth-aware Header here.
 * Phase 1 placeholder pass-through.
 */
export default function AppLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
