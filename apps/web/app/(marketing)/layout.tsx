/**
 * Marketing route group layout. Phase 2a adds Header + Footer chrome here.
 * Phase 1 placeholder pass-through.
 */
export default function MarketingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
